apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: prod
resources:
  - ../../base
patches:
  - path: replica-patch.yaml
configMapGenerator:
  - name: web-config
    behavior: merge
    literals:
      - ENVIRONMENT=prod
generatorOptions:
  disableNameSuffixHash: true
