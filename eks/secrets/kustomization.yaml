apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: web
resources:
  - aws-secret-store.yaml          # ESO -> AWS Secrets Manager (sync-wave 1)
  - web-db-external-secret.yaml    # materializes web-db-credentials (sync-wave 2)
